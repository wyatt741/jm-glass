__pycache__/
.DS_Store
assets/src/
.gate/
docs/qa/*
!docs/qa/side-by-side/
.preflight-backup/

# working papers: on disk and backed up to ../jm-glass-research-private/, but
# never in the repo, because Pages needs a public repo and the client gets that URL
docs/research/
docs/RESEARCH_BRIEF.md

# browser MCP debug output, swept in once by a git add -A
.playwright-mcp/
